<!DOCTYPE html>
<html>
  <head>
    <title>RoR | Blog App | John Palacios</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container max-w-screen-xl px-4 mx-auto my-5">
      <%= render 'partials/header' %>

      <div class="mb-5">
        <% if notice %>
        <p class="block p-2 px-4 mt-5 text-center text-white bg-indigo-400 border md:hover:shadow-2xl md:hover:transition"><%= notice %></p>
        <% end %>
        <% if alert %>
        <p class="block p-2 px-4 mt-5 text-center text-white bg-red-400 border md:hover:shadow-2xl md:hover:transition"><%= alert %></p>
        <% end %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
